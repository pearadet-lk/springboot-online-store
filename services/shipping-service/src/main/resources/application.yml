server:
  port: 5219
spring:
  application:
    name: shipping-service
  config:
    import: optional:classpath:application-shared.yml
management:
  endpoints:
    web:
      base-path: /
      path-mapping:
        prometheus: metrics
