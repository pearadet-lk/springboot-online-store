server:
  port: 5078
spring:
  application:
    name: cart-service
  config:
    import: optional:classpath:application-shared.yml
management:
  endpoints:
    web:
      base-path: /
      path-mapping:
        prometheus: metrics
