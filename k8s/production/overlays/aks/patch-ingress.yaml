apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: online-store-ingress
  annotations:
    cert-manager.io/cluster-issuer: ""
spec:
  ingressClassName: webapprouting.kubernetes.azure.com
  rules:
    - host: api.aks.example.com
